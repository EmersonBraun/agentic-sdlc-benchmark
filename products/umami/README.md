# Umami Brownfield Fixture

The benchmark brownfield fixture is pinned to Umami `v3.3.0` at commit:

`ba2aa48546534c55e9b8174667b4f266fe9d9ea2`

The upstream repository and license are preserved. Benchmark execution uses the controlled Docker environment described in `snapshot-v1.0.json`; upstream CI is retained as provenance and compatibility evidence, not as the local execution environment.
