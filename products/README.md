# Products

This directory contains reproducible product fixtures and snapshot metadata.

- `greenfield/` will contain the benchmark-owned greenfield product.
- `umami/` will contain the controlled brownfield snapshot metadata and mirror instructions.

Product licenses and upstream attribution must be preserved.
