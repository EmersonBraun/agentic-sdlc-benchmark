# Schemas

Versioned JSON Schemas for task manifests, run manifests, ledger events, evaluator results, and processed datasets live here.

Current contracts are `1.0`. Schemas are validated in CI. Schema changes require a protocol or data-contract version bump.
