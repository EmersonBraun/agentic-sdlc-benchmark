# Schemas

Versioned JSON Schemas for task manifests, run manifests, ledger events, evaluator results, and processed datasets live here.

Current contracts are `1.0`; the v1.1 cohort uses
`run-manifest-v1.1.schema.json`. Schemas are validated in CI. Schema changes
require a protocol or data-contract version bump.
